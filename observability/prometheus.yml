global:
  # Scrape aggressively (every second) to catch the rapid connection spikes 
  # during the Thundering Herd Flash Sale simulation!
  scrape_interval: 1s

scrape_configs:
  - job_name: 'mysql'
    static_configs:
      - targets: ['mysql-exporter:9104']
